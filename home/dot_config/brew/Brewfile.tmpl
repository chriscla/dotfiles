# Taps (Repositories)

# Core Binaries (Install on BOTH Linux & Mac)
# These tools usually have the same name on both systems.
brew "git"
brew "chezmoi"
brew "fzf"
brew "ripgrep"
brew "fd"
brew "node"
brew "zsh"
brew "curl"
brew "jq"
brew "bat"
brew "eza"
brew "nvim"
brew "starship"

{{- if .work }}
brew "awscli"
brew "aws-vault"
{{- end }}

# Mac-Specific Logic
if OS.mac?
    # Casks (GUI Apps - Mac Only)
{{- if not .ephemeral }}
    cask "ghostty@tip"
    cask "font-droid-sans-mono-nerd-font"
    cask "finicky"
    cask "spotify"
    cask "bitwarden"
    cask "obsidian"
    brew "tmux"
    
{{- end }}

{{- if .personal }}
# Don't have admin on work machines
    # Mac App Store (Requires 'mas' CLI)
    cask "visual-studio-code"
    brew "mas"
    mas "Tailscale", id: 1475387142
{{- end }}
end

# Linux-Specific Logic (Optional)
if OS.linux?
    # Sometimes tool names differ slightly
    brew "gcc" 
    # Relays Bitwarden's Windows named-pipe agent into WSL2
    # (see ~/.dotfile_extras/bw-ssh-agent-relay.sh). Harmless on native Linux.
    brew "socat"
end

{{- if .trusted }}
brew "gh"
{{- end }}
